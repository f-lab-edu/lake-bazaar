# Repository-wide dotenv example (do not commit real secrets)

# Control-plane / Ansible defaults
ANSIBLE_REMOTE_USER=debian
ANSIBLE_SSH_PRIVATE_KEY_FILE=

# Cloud (OpenTofu/Terraform) via TF_VAR_* or provider-native env
TF_VAR_project_id=
TF_VAR_region=
TF_VAR_zone=

# Prefer ADC: set GOOGLE_APPLICATION_CREDENTIALS or use gcloud auth application-default login
# Leave empty to rely on ADC
GOOGLE_APPLICATION_CREDENTIALS=
TF_VAR_gcp_credentials_file=

# SSH key material (paths)
TF_VAR_ssh_public_key_abs_path=
TF_VAR_ssh_private_key_abs_path=
TF_VAR_ssh_user=debian

# Optional: JSON model for OS users and their SSH keys
# Example:
# [
#   {"name":"lake","groups":["sudo","lake"],"ssh_authorized_keys":["ssh-ed25519 AAA..."],"sudo_nopasswd":true},
#   {"name":"data","groups":["lake"],"ssh_authorized_keys":[]}
# ]
USERS_SSH_MODEL_JSON=
